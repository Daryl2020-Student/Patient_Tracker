{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // AllowedHosts specifies the allowed hosts for the application.
  "AllowedHosts": "*",

  // ConnectionStrings section contains the connection string configuration for the Patient_Tracker_Context.
  "ConnectionStrings": {
    // Patient_Tracker_Context connection string using SQLite as the database provider.
    "Patient_Tracker_Context": "Data Source=MedicalData.db"
  }
}
